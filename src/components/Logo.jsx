import React from 'react';
import logo from '~/../assets/img/logo-full-transparent.png';

export const Logo = ({ width }) => (
    <img className="logo-full" src={logo} alt="Q-Lashes-logo" width={width} />
);
